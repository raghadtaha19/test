import  './to_do_add_delete.css';
import TodoList from './to_do_add_delete';


function App() {
  
    return (
      <div>
      <TodoList/>
      </div>
     
    );
  }
  // In the component's return statement, we define the JSX (JavaScript XML) that will be rendered on the screen
  
  export default App;
  